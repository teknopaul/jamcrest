var jamcrest = (function() {
    function compare(input, matcher, opts) {
        // Stub: always match. Real implementation in Phase 4.
        return { match: true, diagnostic: '' };
    }
    return { compare: compare };
})();
